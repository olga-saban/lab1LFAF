public enum TokenType {
    // Literals
    INTEGER,
    FLOAT,

    // Identifiers & keywords
    IDENTIFIER,
    SIN,
    COS,

    // Operators
    PLUS,
    MINUS,
    MUL,
    DIV,
    ASSIGN,

    // Parentheses
    LPAREN,
    RPAREN,

    // Special
    EOF
}